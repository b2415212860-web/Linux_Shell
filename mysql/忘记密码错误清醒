这个报错 Unable to lock ./ibdata1, error: 11 的原因非常明确：你之前手动启动的 MySQL 安全模式进程（mysqld --skip-grant-tables...）还没有被彻底杀掉！
因为那个旧进程还在后台运行，并且死死锁住了核心数据文件 ibdata1，所以当你尝试用 systemctl start mysqld 正常启动时，新进程抢不到文件锁，就会一直报错。
🛠️ 解决步骤：彻底清理残留进程
第一步：强制杀掉所有 MySQL 相关进程
执行以下命令，把后台残留的 mysqld 进程全部清理掉：
bash

编辑



kill -9 $(ps -ef | grep mysql | grep -v grep | awk '{print $2}')
第二步：确认进程是否清理干净
再次检查是否还有 mysqld 进程在运行：
bash

编辑



ps -ef | grep mysql
如果输出中只剩下你当前正在敲命令的 grep 进程，说明已经清理干净了。
第三步：正常启动 MySQL 服务
现在文件锁已经释放，你可以正常启动服务了：
bash

编辑



sudo systemctl start mysqld
第四步：检查服务状态
bash

编辑



sudo systemctl status mysqld
看到 Active: active (running) 就说明恢复正常了！
💡 补充排查（如果杀进程后仍然报错）：
如果确认进程已经全部杀光，但依然报 error: 11，可能是以下两个隐藏原因导致的：
inode 耗尽：运行 df -i 检查磁盘的 inode 是否已满，这也会导致文件锁定失败。
SELinux 拦截：运行 sudo setenforce 0 临时关闭 SELinux 后重启 MySQL，如果能成功，说明是 SELinux 权限问题。
你先执行第一步把残留进程杀掉，然后再尝试启动服务！

又看到这个 Unable to lock ./ibdata1, error: 11 报错了！这说明你之前手动启动的 MySQL 进程依然没有被彻底杀掉，它还在后台死死锁着数据文件，导致 systemctl 无法正常启动新的 MySQL 服务。

别着急，我们这次用最彻底的方式来清理它：

🛠️ 终极解决步骤：

第一步：找出并强制杀掉所有残留的 MySQL 进程
请依次执行以下命令：
强制结束所有 mysqld 进程
sudo killall -9 mysqld

检查是否还有残留（通过 lsof 查看谁在占用 ibdata1 文件）
sudo lsof /var/lib/mysql/ibdata1
如果 lsof 命令输出了某个进程 ID（PID），请执行 kill -9  把它杀掉。如果没有任何输出，说明文件已经释放。

第二步：清理残留的 PID 文件（关键）
有时候进程虽然死了，但 PID 文件还在，导致系统误以为服务还在运行：
sudo rm -f /var/lib/mysql/*.pid

第三步：检查磁盘 inode 是否耗尽（隐藏原因）
如果进程确实杀干净了但还是报 error: 11，可能是磁盘的 inode 满了，导致无法创建文件锁：
df -i
检查 Use% 是否达到了 100%。如果满了，需要清理一些无用的小文件。

第四步：正常启动 MySQL 服务
确认以上都没问题后，再次启动服务：
sudo systemctl start mysqld

第五步：验证状态和端口
sudo systemctl status mysqld
ss -tlnp | grep :3306

你先执行第一步的 killall -9 mysqld 和 lsof 检查，确认后台真的没有 MySQL 进程在偷偷运行，然后再启动服务！